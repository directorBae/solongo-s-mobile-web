import axios from "axios";
import { Place, Pathfind, Pallete } from "../store/types/types";

const agentApi = axios.create({
  baseURL: "http://localhost:3000/api",
});

setTimeout(() => {
  return {};
}, 1000);
